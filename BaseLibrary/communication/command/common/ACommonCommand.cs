namespace BaseLibrary.communication.command.common {
    /// <summary>
    /// Class for all common command. Common commands are every command witch can do every robot.
    /// </summary>
    public abstract class ACommonCommand : ACommand{
    }
}
